# Legacy Server Spike

This `server/` directory is an earlier auth-focused prototype that predates the current working application in `backend/`.

Current active paths:
- backend: `../backend`
- frontend: `../client`

Use `backend/` for all current backend implementation, testing, and documentation work.

This folder is kept temporarily for reference during cleanup and should not be treated as the source of truth for the active app.
